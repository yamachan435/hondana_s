<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :isbn %>
    <%= form.text_field :isbn %>
  </div>

  <div class="field">
    <%= form.label :holder %>
    <%= form.text_field :holder %>
  </div>

  <div class="field">
    <%= form.label :registerer %>
    <%= form.text_field :registerer %>
  </div>

  <div class="field">
    <%= form.label :duedate %>
    <%= form.date_select :duedate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
